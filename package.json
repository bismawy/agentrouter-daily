{
  "name": "agentrouter-daily",
  "version": "1.0.0",
  "description": "Daily auto-claim $25 reward for agentrouter.org via Cloudflare Workers Cron",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bunx wrangler dev",
    "deploy": "bunx wrangler deploy",
    "typecheck": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/playwright": "^1.3.5",
    "@cloudflare/workers-types": "^4.20240405.0",
    "typescript": "^5.4.5",
    "wrangler": "4"
  }
}
